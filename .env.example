# ── Motor local (npm run analisar / bootstrap) ──────────────────────────────
API_FOOTBALL_KEY=
ODDS_API_KEY=

# ── Dash (Vite) — só a publishable key vai pro browser ──────────────────────
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# A SECRET key (sb_secret_...) NUNCA entra aqui nem no front: ela vive só nos
# secrets das edge functions (supabase secrets set) e no ambiente do migrar.
