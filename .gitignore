node_modules
dist
.env
*.local
.DS_Store

# Local Netlify folder
.netlify

# Snapshots de dados de aposta — histórico financeiro não vai pro git.
backup/

# Harness de medicao de viewport — gerado, nao versionado.
viewport.html
src/_viewport.tsx

# Conteúdo real do bootstrap manual dos analistas (o .exemplo.json fica versionado).
data/analistas-manual.json
