// Tipos do módulo JS puro veredito.js — deixa o dash (TS estrito, moduleResolution bundler)
// importar a fonte única sem allowJs e sem duplicar a lógica em TypeScript.
export function vereditoOdd(args: { prob: number; odd: number; evMinimoPct?: number }): {
  justo: number;
  valorNaOdd: number;
  ganhoPct: number;
  vale: boolean;
};
